import math


def differenceFull(current, target):
    w, h=target.shape[0], target.shape[1]
    for (x=0):
    pass

def differencePartial(current, target, primitive, score):
    w, h=target.shape[0], target.shape[1]
    
    total=math.pow(2, score)*w*h*4
    
    
    
def drawShape(current, primitive, colour, height_map):
    pass
